---
name: qdd-phase
description: Use when the user types /qdd-phase with a single phase number or all to execute one QDD phase or the entire active plan.
---

# QDD Phase Command

Follow the shared workflow in `../qdd-workflow/SKILL.md`, specifically the `/qdd-phase {N}` and `/qdd-phase all` sections.
Apply `docs/rules/prompt-quality.md`: execute the requested phase target, keep implementation scoped, record verification, and leave independent review to `/qdd-review`.
